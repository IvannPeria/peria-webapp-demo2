import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Tailwind UI Playground | Build Stunning UI Fast",
  description: "An interactive playground to explore Tailwind CSS. Experiment with background colors, border radius, and shadows in real-time.",
  keywords: ["Tailwind CSS", "UI Design", "Frontend Development", "Interactive Playground", "CSS Generator"],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
